# Runtime env vars for the frontend (web) container.
# NOTE: NEXT_PUBLIC_API_URL is baked into the client bundle at build time
# (see apps/web/Dockerfile ARG) — changing it here has no effect after the
# image is built. Only server-only, non-NEXT_PUBLIC_* runtime vars belong here.
