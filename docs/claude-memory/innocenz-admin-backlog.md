---
name: innocenz-admin-backlog
description: Next-session backlog + key data-model finding for the InnocenZ apps/web admin portal UI/UX work
metadata: 
  node_type: memory
  type: project
  originSessionId: 0ccd2ebf-f34b-4f04-ae14-07954eaaae41
  modified: 2026-07-30T07:09:15.580Z
---

Admin portal work targets **apps/web** (the Nx app at localhost:3000), NOT InnocenZ-proto. The proto + `InnocenZ_MVP_v3_Final.xlsx` are the design/spec reference. SOS is removed from InnocenZ; PV-dispute review is Agency-side (not an Admin function).

**Done + merged (PR #1, branch jk, 2026-07-16):** dashboard donut charts (donut-chart.tsx, dashboard.tsx), live sidebar badges (use-sidebar-badges.ts), RBAC tab bar Role/Permission/Module/Pending + new /rbac/pending approvals inbox (rbac/route.tsx, rbac/pending.tsx), PR table many-agencies fix = 2 chips + "+N more" popover (prs-table.tsx). All type/lint clean; styled via shared design tokens + PageShell/PageHeader.

**Done this session (branch jk, 2026-07-16, uncommitted):**
- **#1 Agency admin fields** — new `components/organization/agency-details.tsx` (labeled grid: Owner/Email/Phone/SSM-Org-No/Agency code, email→mailto & phone→tel links) rendered at top of the expanded agency row in `agencies-table.tsx`; also fixed the compact Contact cell to show BOTH email + phone (was `email || phone`, dropped phone when email present).
- **"Reset demo" header button** — `components/layout/header.tsx` (rendered by admin-layout.tsx): added `ResetDemoButton` in the top-right cluster (before ThemeToggle) that calls `queryClient.invalidateQueries()` to refetch ALL queries + toast "Demo data refreshed". It's a client-side view refresh only — it does NOT re-seed the demo DB. Backend seed data lives in CLI scripts `apps/backend/src/scripts/seed-sample-*.ts` (no HTTP endpoint); a true reset would need a new (destructive, gated) backend endpoint to run those.
- **Plan Request identity is now read-only** — `routes/(admin)/service/requests.tsx`: Who (`subscriberName`), Role (`subscriberType`), Type (`type`) were editable (Input/Select calling `updateAdminRequest`), which let admin rewrite/reclassify the requester and skew the byRole negotiated-summary. Now render as read-only text/badges (Who shows `contactName` as subtext; roleBadgeColors/roleLabels added). Admin edits only Remarks + Quoted(RM) + Status/Resolve. `updateFieldsMutation` narrowed to `{id, remarks}`; removed draftWho/saveWho. Rationale: the request row is the immutable record of the Outlet/Agency action that created it (agency 151+ PV → "Renegotiate Price"; outlet POS "Request admin quote"). (NOTE: analogous initiatedBy/source-name fields in /service/other's edit drawer were left editable — those are admin-curated job orders, not user requests.)
- **PR page agency UX** — `components/pr/prs-table.tsx`: (a) replaced the plain agency-filter `Select` with a searchable combobox `AgencyFilterCombobox` (Popover + Input + filtered list, matches on name or `agencyCode`, Check on selected; no cmdk primitive exists so built from existing Popover/Input); (b) replaced the truncated agency chips + ">2 more" popover with `PrAgenciesCell` — a per-row dropdown button (first name + `+N` + chevron) whose Popover lists EVERY agency's full name (break-words, untruncated) + code. Fixes "some agency org names hidden/truncated". No prop-signature change to PrsTable, so route caller untouched. tsc + biome clean.
- **Special-services = both outlet AND agency post-job flow.** The `special_service` model ALREADY supports it: `initiatedBy` (outlet|agency), `adminAccepted` (n_a|pending|accepted|declined — "agency posts need admin accept/decline, outlet posts go live"), `postingAgencyName` vs `assignedAgencyName`. Admin `/service/other` has a "Pending review" view via `fetchAdminPendingJobs` (adminAccepted='pending'). The ONLY gap was demo data: `seed-sample-special-services.ts` created outlet-only rows (initiatedBy default 'outlet', adminAccepted 'n_a'), so Pending-review card was always 0. FIXED: added 4 agency-initiated rows (2 adminAccepted='pending', 1 accepted, 1 declined) via new Seed fields initiatedBy/adminAccepted/postingAgency + postingAgency name lookup in the insert. Now 10 orders total, and the agency post-job → admin-review flow is visible. (Still must RUN the seed: `npx tsx --tsconfig tsconfig.json src/scripts/seed-sample-special-services.ts` from apps/backend; needs seed-sample-orgs first for outlet/agency id resolution.)
- **Plan page "Coverage" column** — `components/subscription/subscriptions-table.tsx`: added a Coverage column (after Price) showing each plan's volume tier from the proto rate cards via static `PLAN_COVERAGE` lookup keyed by role+name (agency PV/week: Starter 5, Plus 6–10, Growth 11–25, Enterprise 26–75, Scale 76–150, Custom 151+; outlet PRs/day: Essential 5, Plus 6–10, Pro 11–25, Enterprise 26–50, Scale 51–100, Premier 101+). Keyed by role because Plus/Enterprise/Scale exist for both with different ranges/units. Prices already matched proto (from seed-plans.ts) — no change. colSpan 7→8. NOTE: user's wording said "pv/month"+"prs/week" but proto (authoritative) uses PV/week + PRs/day — followed proto.
- **Coverage is now an EDITABLE persisted field (done)** — user asked why the Edit Plan drawer had no coverage field. Made it a real column end-to-end: backend `subscription.model.ts` added nullable `coverage varchar(100)` (NEEDS MIGRATION — run `pnpm migrate` from apps/backend to generate+apply); backend `subscription.schema.ts` SubscriptionSchema + `subscription.controller.ts` create/update persist it (response already spreads `...subscription` so it flows out); `seed-plans.ts` PlanSeed gained `coverage` with proto values per plan (re-run seed-plans to populate). Frontend: `services/subscription/types.ts` Subscription gained `coverage: string|null`; `schemas.ts` SubscriptionSchema gained `coverage` (→ CreateSubscriptionInput); `subscription-form-sheet.tsx` (Create/Edit Plan drawer) has a "Coverage" Input (defaultValues + useEffect setFieldValue + field); table cell shows `sub.coverage || coverageFor(sub)` so the proto lookup is the fallback until seeded/edited. tsc+biome clean. To fully activate: `pnpm migrate` then re-run seed-plans.ts.
- **Jobs & Special Services "Third Party" reframe + columns** — per user: admin does NOT assign an InnocenZ agency; they view the request and find an external THIRD PARTY. `routes/(admin)/service/other.tsx`: renamed column "Assigned Agency"→"Third Party", "Scheduled"→"Scheduled For", added "Requested Time" column (`formatDate(record.createdAt)`) after Scheduled (colSpan 7→8); drawer meta dt labels renamed Assigned agency→Third party, Scheduled→Scheduled for, Created→Requested time. Value still stored in `assignedAgencyName` (repurposed as the third-party vendor name). Seed `seed-sample-special-services.ts`: renamed Seed field `assignedAgency`→`thirdParty`, values now external vendors (Glow & Co Makeup Studio, RapidCover Staffing, MetroRide Transport, Atelier Threads), insert sets `assignedAgencyId: null` + `assignedAgencyName: s.thirdParty` (third parties aren't agencies). Re-run the special-services seed to see vendor names. Third-party field is now EDITABLE (done): backend `special-service.schema.ts` UpdateSpecialServiceSchema gained `assignedAgencyName: z.string().max(255).optional().nullable()`; controller `update` whitelists it into payload (repo.update already accepts it via the assign path); frontend `services/special-service` UpdateSpecialServiceInput gained `assignedAgencyName?: string|null`; OrderEditForm has a `thirdParty` state + editable "Third party (optional)" Input (moved out of the read-only dl), diffed in handleSubmit (empty→null). Admin can now write/clear who supports the category. tsc+biome clean.
- **Source filter = 2 left-side toggle buttons** — new shared `components/admin/source-toggle.tsx` (Outlet/Agency segmented buttons; click active clears to "all"). Used on FOUR pages now: Plan Request (roleFilter), Jobs & Special Services (sourceFilter, hidden in pending view), Plan/`subscriptions-table.tsx` (replaced the All Roles/Agency/Outlet Tabs; roleFilter is a role ID so an adapter maps outlet/agency↔roleOptions id via `roleSourceValue`/agencyRoleId/outletRoleId), and History/`business/history.tsx` (subscriberTypeFilter). Headers restructured to `space-y-4` (title above), toggle gets `sm:mr-auto` to sit left while the other dropdowns stay right. No "All" button by design (2 buttons only). A plain `<div>` container — no role/aria-label (biome a11y).
- **Plan Request → read-first + edit drawer** — `routes/(admin)/service/requests.tsx` fully rewritten to match the /service/other UX: table rows are now read-only (Who/Role/Type/Remarks/Current Plan/Quoted/Status/Requested, removed the inline Remarks input, Quoted input, and per-row action buttons/column → colSpan 9→8), row is `cursor-pointer` + `onClick={() => setEditRequest(request)}`. New `RequestEditForm` inside a `<Sheet side="right">` (keyed by request.id): read-only requester `<dl>` (Who/Contact/Role/Type/Current plan/Requested), editable Remarks `<Textarea>`, Quoted (RM) `<Input>` (editable only when `isPriceEditable`, else read-only fixed price with caption), and a Status section with "Mark contacted" (if pending) + "Resolve"/"Resolve with quote" buttons; footer Cancel + Save changes (saves remarks via updateFieldsMutation, disabled unless remarks changed). Removed draftQuotes/draftRemarks/resolveTarget/editingId + ResolveRequestDialog usage; each action calls mutateAsync then onDone() to close. tsc + biome clean.
- **Conditional Quoted (RM) editability** — `routes/(admin)/service/requests.tsx`: added `isPriceEditable(request)` = `type==="pos_integration_quote"` OR (`type==="plan_change"` && `subscriberType==="agency"`). Only those rows render the editable RM `<Input>` + "Set quote" (→ResolveRequestDialog). Every other row (outlet plan_change, contact, other) is FIXED: the Quoted cell shows the current plan's set price read-only (`RM {planById.get(currentPlanId).price}` + "Fixed price" caption; added `planById` map alongside `planNameById` from fetchSubscriptions which carries `price`), and the action is a plain "Resolve" button that calls `resolveMutation.mutate({id, quotedAmount: undefined})` (fixed rows don't count toward the byRole negotiated summary — correct). Rule per user: outlet plan = fixed except POS "request admin quote" (editable); agency = auto/fixed except 151+ PV Custom tier (editable). tsc + biome clean. Seed `seed-sample-activity.ts` ADMIN_SEED now also has 2 outlet plan_change rows (Onyx Speakeasy/Scale, Crimson Terrace/Plus) to demo the fixed-price read-only case (total 16 requests).
- **Expanded demo seed** — `apps/backend/src/scripts/seed-sample-activity.ts`: `ADMIN_SEED` 5→14 (Plan Request page) and `MEMBER_SEED` 6→14 (History/subscriptions). New rows encode the pricing rule: only outlet `pos_integration_quote` + agency `plan_change` at 151+ PV (Custom tier) carry a `quotedAmount`; `contact`/`other` never do. New subscribers use existing plan-catalog names (outlet Plus/Scale/Premier/Pro; agency Scale/Custom/Growth). Script is idempotent (wipes `createdBy='seed-sample'` first), so re-running `npx tsx --tsconfig tsconfig.json src/scripts/seed-sample-activity.ts` from apps/backend IS the effective "reset+reseed" of demo activity (the header Reset-demo button only re-fetches the view).
- **#3 /service/other redesign** — `routes/(admin)/service/other.tsx` rewritten read-first: every cell now plain text/badge (Title, Source name + role badge, Category badge + `others` description, Budget `RM …`, Assigned Agency, Scheduled, Status chip). Editing moved to a right-side `Sheet` drawer (OrderEditForm) opened by row-click or the pencil button — edits Title, Source type/name, Category, Description, Budget, Status; diffs against the record and fires `updateSpecialService` (fields) and/or `updateSpecialServiceStatus`. Removed all per-row draft state (draftBudgets/Titles/Sources/Descriptions + save handlers). NOTE: the top filters (Sources/Categories/Status) were ALREADY wired to queryParams — that part of the old note was stale. Verified: tsc clean (my files), biome clean, `vite build` succeeds (SSR bundles other-*.mjs + organization-*.mjs).

- **#2 PR legal name / IC — DONE (was mislabeled BLOCKED).** The old note was wrong: the legal identity already exists as `user_profile.firstName/lastName/idType/idNo` (idType enum NRIC/Passport/Work permit) and the `GET /user` list endpoint ALREADY nests it as `profile:{…}` via `withUserProfiles` (apps/backend user.controller.list). So it was frontend-only: (a) `services/admin/mappers.ts` — added `BackendUserProfile` + optional `profile` on `BackendUser`; (b) `services/pr/types.ts` — `PrUser` gained `legalName`, `idType`, `idNo`; (c) `services/pr/prs.ts` — `mapPrUser` derives `legalName = [firstName,lastName].filter(Boolean).join(" ")`, maps idType/idNo; `matchesSearch` now also matches legalName + idNo; (d) `components/pr/prs-table.tsx` — new "Legal name" column (legal name + `idType · idNo` subtext, **unmasked** per user), colSpan 6→7. Backend seed `seed-sample-prs.ts` now populates firstName/lastName/idType/idNo (4 NRIC + 1 Passport) so the demo shows values — re-run it to see them. NOTE: `services/pr/*.ts` had corrupt mixed `\r\r\n` line endings — edit them via PowerShell `[IO.File]::ReadAllText/WriteAllText` with single-line anchors (Edit tool's LF match fails), then `biome check --write` to normalize.

- **Per-tab auth pinning (2026-07-30, commit `3e45550`) — the portal-jump/phantom-signout fix.** All portals shared one localStorage token slot, so logging into outlet in a 2nd tab silently replaced the agency tab's identity → role guard bounced it. `apps/web/src/lib/auth/auth-storage.ts` REWRITTEN: tokens live in per-tab sessionStorage pinned via an `auth_seeded` flag; localStorage only SEEDS brand-new tabs (same key names access_token/refresh_token/token_expiry so existing logins survive); `writeKey` sets pin+tab+seed, `removeKey` clears both. It is the ONLY file touching those keys (grep-verified). Each tab is now static to its login; only a login/sign-out in that tab changes it.

**Remaining backlog:**

**Env notes:** editor GateGuard hook forces a facts-checklist on every file write (state importers/affected API/schema/instruction, then retry). `apps/web/biome.json` = tabs + double quotes. Typecheck via `npx tsc --noEmit -p tsconfig.json --ignoreDeprecations 6.0` (bare tsc errors on deprecated baseUrl). New routes need `npx tsr generate`.
