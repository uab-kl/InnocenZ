---
name: outlet-post-job-next
description: "Next task: wire outlet Post Job. Blocked on an undecided product call — outlet write access vs a request flow"
metadata: 
  node_type: memory
  type: project
  originSessionId: 14f9e60c-0d13-44a4-80ff-cdb6f7116063
  modified: 2026-07-21T03:43:56.769Z
---

Agreed 2026-07-20 to do **outlet Post Job in a fresh session**. Everything else on the outlet's read-only surface is already wired — see [[outlet-shift-read-access]] and [[outlet-portal-backend-wiring]].

**Screen:** `routes/outlet/bookings.tsx` (note: `/outlet/bookings` is Post Job; the Calendar is `/outlet/ratings`. Route filenames do NOT match their nav labels — I got this wrong once already.) It's a draft-shift composer: `composer` + `draftShifts` local state → `createShifts(...)` on the demo store. Two tabs: PR Shift and Services (Services is already backend-wired via `useOutletSpecialServices`).

**DECIDED 2026-07-21: outlets write shifts DIRECTLY** (option 1). Rationale from the user — making shifts and requesting PRs from agencies is the outlet's primary use of the app. So: widen shift POST/PUT/DELETE to the `outlet` role, scoped to the outlet's own venue, with the agency derived from `outlet.onboardedByAgencyId`. No request-flow layer. This also unblocks the Today live-ops writes (check-in, release early, cut-loss) under the same auth change.

**FRONTEND WIRED + BACKEND LIVE-VERIFIED 2026-07-21 (branch SL, uncommitted).** Reverse mapper `createShiftInputFromPost()` + `OutletShiftPostItem` type added to `backend-shift-map.ts` (drops demo-only fields with no shift column). New hook `use-outlet-post-job.ts` (`{backed, postShifts, isPosting}`, posts sequentially, invalidates `["outlet"]`+`["roster"]`). `bookings.tsx` `submitNew` now branches: `backed` → `postShifts(postItems)` to the API; else demo `createShifts`. Submit disabled while `isPosting`. All 3 files tsc-clean (web app has MANY pre-existing unrelated tsc errors — build is `vite build`, no tsc gate). **Live API test PASSED** (owner@velvet23.my/Password123!, outletId ed739c13…, derived agency c30fcd15…=Atlas): POST no-agencyId→201 derived; POST forged zero-uuid agencyId→ignored, derived wins; foreign outletId→blocked; outlet DELETE→200. **NOT yet done: browser UI click-through** (in-app browser blocked navigating to the externally-run :3000 dev server — origin-approval gate; do it in an interactive session) and **NOT committed**. ~~KNOWN CAVEAT: subscription-cap counting reads empty demo `shifts` on a backed session.~~ **FIXED 2026-07-21 (branch SL, uncommitted) — this was "section A / A3".** `use-outlet-post-job.ts` now also fetches the outlet's real shifts (today→+120d) + assignments and returns `bookedShifts: {outletName,dateIso,quantity,requestedPrIds}[]` (requestedPrIds = assigned non-cancelled/no-show PR ids, the persisted proxy since posted shifts drop demo requestedPrIds). `bookings.tsx` computes `capShifts = backed ? bookedShifts : shifts` and feeds it to ALL FOUR cap-counter sites (2 composer memos + the 2 submit-validation checks). tsc-clean both files; demo render confirmed (no console errors); backed branch mirrors the live-verified useOutletToday fetch/map but NOT itself browser-verified with a real login. NOT committed.

**BACKEND DONE 2026-07-21 (branch SL, uncommitted):** the auth change is implemented. `shift.routes.ts` `canWrite` now = `admin|agency|outlet`. `ShiftControllerClass` gained an `isOutletCaller()` helper + `outletRepository` injection (wired in composition-root). `create`: outlet caller must post to an outlet in its own `scope.outletIds`, and `agencyId` is DERIVED from that outlet's `onboardedByAgencyId` (any client-supplied agencyId is ignored; null onboarding agency → 400). `update`/`remove`: ownership now matches admin OR agency-on-`agencyId` OR outlet-on-`outletId`; outlets can't reassign agency or move a shift to another venue (`data.outletId`/`data.agencyId` stripped). tsc clean (2 pre-existing `pr.repository.ts` portfolioPhotos errors are unrelated). **NOT verified live / not committed.** FRONTEND Post Job still demo-only — see reverse-mapper notes below; no live API verification done yet.

**B (sales capture backend) + C (Reports wiring) DONE + LIVE-VERIFIED 2026-07-21 (branch SL, uncommitted).**
- **B backend:** new `shift_sale` feature — table `main.shift_sale` (per-(shift,PR) drink/tip/table RM + units + total, outlet/agency/sold_on denormalized from shift, unique(shift_id,pr_id)). Migration `0037_add_shift_sale.sql` HAND-AUTHORED (generate is broken — snapshots stop at 0019, all 0020+ are hand-written; journal `when` must exceed 0036's 1784500000020, used ...021) and APPLIED to shared DB. Files: shift-sale.model/repository/controller/routes + schema/shift-sale.schema.ts + composition-root + router/v1. Endpoints (role admin|agency|outlet, controller scopes each caller): `POST /shift-sale` (upsert; outlet/agency/soldOn DERIVED from shift, total computed server-side, PR must be assigned → else 400), `GET /shift-sale`, `GET /shift-sale/report` (→ {byDay, byPr}). Live test PASSED: derived fields correct, forged total/outlet ignored, upsert overwrites, unassigned-PR→400, report aggregates. Revenue side only; PR COST stays on shift_assignment.pay_amount.
- **C Reports wiring:** frontend service `services/shift-sale/index.ts` + hook `use-outlet-sales-report.ts` (fetches /shift-sale/report for sales + /shift-assignment for cost, exposes buildReport/buildFloorBreakdown/buildTopPrs over a date range). `OutletSalesDashboard.tsx` now branches on `backed` (real outlet session) → backend report/floor/topPrs/prSpend; demo velvet path only in unauthenticated mode. All changed files tsc-clean (also fixed 1 recharts formatter any-type). Data inputs live-verified compatible (byDay 3-day split + byPr + assignment shiftDate/prName/payAmount).
- **COMMITTED 2026-07-21 as `87f1573` on branch SL (NOT pushed).** Single commit covers both the Post Job direct-write work AND the shift_sale B+C. Formatter-churn file `OutletShiftDetailPanel.tsx` was `git checkout`-reverted before commit (biome reformats single-quote/space → double-quote/tab; the web app's canonical style IS biome tabs+double-quotes, but that file had no logic change so it was left out).
- **CODE REVIEW findings — ALL 5 DONE + COMMITTED 2026-07-21 as `d5b7a38` on branch SL (NOT pushed).** Single commit covers findings #1/#3/#4/#5 + the #2 table-sales drop. (Formatter-churn file `routes/outlet/bookings.tsx` — a whole-file biome reformat that appeared in the working tree — was left UNSTAGED, not reverted, since `git checkout --` was classifier-blocked; it's harmless quote/indent churn, still uncommitted in the tree.)
  1. ~~[MEDIUM] Reports PR-cost capped at 100 assignments.~~ **DONE.** New `ShiftAssignmentRepository.reportCostByPrDay(filter)` aggregates `pay_amount` grouped by (pr_id, pr_name, shift_date), status NOT IN (cancelled,no_show). `/shift-sale/report` now returns `{ byDay, byPr, costByPrDay }` (dropped the window-collapsed costByPr idea — kept **(PR × day) grain** so the hook can still slice ANY selected range client-side; wide-fetch design preserved, no component reorder). `use-outlet-sales-report.ts` consumes `report.costByPrDay`, dropped the capped `fetchShiftAssignments({pageSize:100})` fetch + `isStaffing` (server excludes now). tsc-clean both apps (backend only pre-existing pr.repository portfolioPhotos errors; web 190 pre-existing, none in my 3 files). Aggregate SQL live-run against shared DB → correct per-(pr,day) rows; `shift.shiftDate` is `date({mode:'string'})` so `soldOn` keys match the sales side. NOT live-API/browser verified (server not started this session).
  2. ~~[LOW] Floor "Tips" column folds table sales.~~ **RESOLVED 2026-07-21 by DROPPING table sales entirely** (user: "there should be no such thing as tips for tables"). Removed `table_sales_rm` from the whole backend floor-sales model — schema (CreateShiftSaleSchema), model column + ShiftSaleDayTotals, repository upsert + reportByDay, controller total (now drink+tip only), web `services/shift-sale` (ShiftSale/ShiftSaleDayTotals/LogShiftSaleInput), and the hook's `buildFloorBreakdown` (Tips = real tips only). Migration `0039_shift_sale_drop_table_sales.sql` recomputes `total_sales_rm = drink+tip` for existing rows then drops the column — **APPLIED to shared DB + column-drop verified**. tsc clean both apps. ~~STILL demo-only: 6 demo-store files keep a `tableSales` concept.~~ **SCRUBBED 2026-07-21 (branch SL, uncommitted).** Removed the table-SALES input from `calcShiftPayout` (agency-demo.ts) — `tableCommission` is now a hardcoded `0` (KEPT in the return + on persisted objects so PV/history/receipt-scan consumers don't cascade), and deleted every `tableSales:` arg + the dead "Commission – Tables" PV row across all 6 files (`agency-demo.ts`, `agency-actions.ts`, `outlet-financial-sync.ts` [also dropped `+payout.tableCommission` from 2 sums], `shift-history-amounts.ts`, `portal-sync.ts`, `OutletSealReview.tsx`). `grep tableSales` = 0 hits; tsc-clean; outlet Reports renders real figures, no console errors. **`tablePct` (table-commission RATE) — DROPPED backend-side 2026-07-21, migration 0040.** `0040_outlet_drop_table_pct.sql` (journal idx 40, when 1784500000024) drops `table_pct` from `outlet_workspace` AND `outlet_tier_rate`; **APPLIED to shared DB via bare `drizzle-kit migrate --config=drizzle.migrate.config.ts` (NOT the `migrate` npm script — it runs the broken `generate` first) and VERIFIED (0 table_pct columns).** Removed from backend model/controller/schema; web `services/outlet-workspace` types + `outlet-workspace-map.ts` no longer read/send it — the FRONTEND `OutletWorkspaceSettings.tablePct` field is KEPT but defaulted to 0 (same pattern as tableCommission=0), so the ~40 demo consumers didn't cascade. tsc clean both apps (only pre-existing pr.repository portfolioPhotos errors). **`perTableRm`/`per_table_rm` — DROPPED backend-side 2026-07-21, migration 0041** (`0041_outlet_drop_per_table_rm.sql`, journal idx 41 when ...025). Only `outlet_workspace` had it (NOT outlet_tier_rate). APPLIED to shared DB + VERIFIED (0 columns). Same tablePct pattern: removed from backend model/controller/schema + web `services/outlet-workspace` types + `outlet-workspace-map.ts` read/write; FRONTEND `OutletWorkspaceSettings.perTableRm` field KEPT, map-read defaulted to 0 (store.ts + outlet-demo.ts `DEFAULT_PER_TABLE_RM` still reference it, no cascade). Committed `eb0f8c7`. **STILL NOT removed (frontend-only, no backend column — a demo-data cleanup, not a migration):** the persisted `tableCommission`/`tableCommissionRm` fields (ReceiptScan, PV lines, sealed shifts, store PV totals, demo-seed).

**`tableCommission`/`tableCommissionRm` FIELDS — FULLY REMOVED 2026-07-21, committed `b3addc5`.** Frontend-only (no backend column). Stripped end-to-end: receipt engine (`calcReceiptCommissions`, `ReceiptScan`), PV "Commission – Tables" row, payout/history breakdowns (`shift-history-amounts`, `shift-history`, `history-demo-sync`), store PV type+totals, reconciliation checks, seeds, `ReceiptScanSlip`. `grep tableCommission` = 0 hits; history page live-verified (renders payout figures, no console errors). **KEPT (out of scope — a SEPARATE "table units" subsystem, ~20 refs):** `tableUnits`/`totalTables`/`RECEIPT_COMMISSION_RULES.tablePerUnit` + the receipt `'tables'` category (active logic maps it to drinks). I over-removed `tablePerUnit` mid-task and had to restore it — the tableUnits counters (`sealedShiftCommissionRm`, `shiftSalesTargetRm`, receipt 'tables' item building) still use it. Scrub that subsystem next if the user wants tables gone entirely.

**TWO LESSONS (cost me real time this session):**
1. **Web typecheck = `tsconfig.json`, NOT `tsconfig.app.json`.** Earlier `npx tsc -p tsconfig.app.json` runs SILENTLY no-op'd (path doesn't exist, exit 2, empty filter → false "clean"). Always: `cd apps/web && npx tsc --noEmit -p tsconfig.json`. It has ~130 PRE-EXISTING errors (TanStack Router route-typing + noUnusedLocals on demo files); vite build doesn't gate on tsc. Judge NEW errors by filtering to touched files / non-TS6133 codes.
2. **Vite HMR gets stuck after many rapid edits** — the long-running dev tab froze at one `?t=<ts>` module version and threw `ReferenceError: tableCommission is not defined` from STALE compiled modules even though source was clean. A cache-busted `fetch('/src/.../file.ts?bust='+Date.now())` proved the served transform was clean; a **FRESH browser tab** loaded with zero errors. Verify in a new tab (or after a dev-server restart), not the edited-all-session tab.

**CAVEAT — biome formatter churn (RESOLVED):** committed `2815fb4` (style: biome tabs/double-quotes/import-reflow on OutletSealReview, OutletShiftDetailPanel, agency-actions, outlet-financial-sync). Original note: after committing `642c29c`, biome reformatted 7 of those files (tabs/double-quotes, NO logic) — left UNSTAGED in the working tree (OutletSealReview, OutletShiftDetailPanel, agency-actions, agency-demo, outlet-financial-sync, portal-sync, shift-history-amounts). Canonical style IS biome, so these can be committed as a pure-format commit or `git checkout`-discarded; harmless either way.

**COMMITTED 2026-07-21 on branch SL (NOT pushed, now 4 ahead of origin/SL):** `af9c945` feat(outlet): subscription caps backed by real shifts + Confirm staffing wiring (A3 + confirm); `642c29c` refactor: remove table-sales & table commission end-to-end (6-file scrub + migration 0040 + outlet-workspace backend/web). Pre-existing bookings.tsx formatter churn went into af9c945.
  3. ~~[LOW] POST /shift-sale lets cancelled/no_show PR carry sales.~~ **DONE** — create() now requires an assignment with `status !== cancelled && !== no_show` (message "PR is not actively assigned to this shift").
  4. ~~[LOW] No index for the report query.~~ **DONE** — index `shift_sale_outlet_sold_on_idx` on `(outlet_id, sold_on)` declared in shift-sale.model + migration `0038_shift_sale_report_index.sql` (journal when 1784500000022). **APPLIED to shared DB + verified in pg_indexes.**
  5. ~~[LOW/DRY] resolveScope/isOutletCaller copied in 3 controllers.~~ **DONE** — extracted `src/util/org-scope.ts` (`OrgScope`, `resolveOrgScope(req, deps)`, `isOutletCaller`); shift, shift-assignment, shift-sale controllers now delegate (pass explicit `{authRepository, agencyMemberRepository, outletMemberRepository}` since `this` isn't structurally assignable through private fields). Behavior identical, tsc-clean.
- **NOT done:** browser UI click-through (in-app browser still blocked on the externally-run :3000 dev server — origin gate; interactive session needed). Deferred: wowGrowthPct=0 (no prior-period endpoint); the Today "log sales" drink-counter still writes the demo store (write-side wiring of `logShiftSale` needs a per-PR picker — `logShiftSale` service already exists); cut-loss/release-early stays the demo approval flow (not needed by Reports). Shared DB now has ~4 seeded velvet shift_sale rows (2026-07-14/15/17).

**IMPLICATION — the live-ops SALES capture is a genuinely missing backend (confirmed 2026-07-21).** The outlet Reports screen (`routes/outlet/billing.tsx` → `OutletSalesDashboard`, "Weekly earnings, P&L split & top PRs") needs per-PR, per-shift sales figures (drink/tip/table amounts). Checked every table: `shift_assignment` has `pay_amount` + check-in/out but NO sales breakdown; `outlet_workspace` holds only the RATES (drink %, tip %, per-drink RM); `outlet_transaction` is a generic ledger. **The floor-sales data exists nowhere — it is demo-only in the store.** So wiring Reports (user asked for this 2026-07-21, item #3) REQUIRES first creating a per-shift/per-PR floor-sales table + write endpoints (the live-ops side of #2), then aggregation read endpoints. #3 depends on #2b.

**"Wireable things" sweep — 2026-07-21 (branch SL, uncommitted).** After A3, asked to "complete all the wireable things." On inspection only ONE more was true pure-wiring:
- **Confirm staffing — DONE.** New hook `use-outlet-shift-actions.ts` (`useOutletShiftActions` → `{backed, confirmShift, isConfirming}`) calls `updateShift(id,{status:'confirmed'})` + invalidates `["outlet"]`/`["roster"]`. `OutletShiftDetailPanel.tsx` now branches: backed → backend confirm (button disabled while `isConfirming`); demo → old store `confirmShift`. tsc-clean; outlet home renders no console errors; NOT browser-verified with a real login (confirm button hidden in demo since the seed shift is already 'confirmed'). NOT committed.
- **Commission = NOT wireable (it's a build).** `commission_config` backend IS mounted (`/commission-config`, per-(outletId,agencyId,itemType,commissionRate)), but there is NO frontend service, the agency-profile `outletCommissionRules`→config mapping is unclear, AND the agency Profile save is itself demo-only (`saveAgencyProfileSettings`/`saveAgencyOwner` write the store; use-agency-profile has no commission handling). Needs service+hook+save rewiring.
- **Subscription renegotiate = NOTHING to wire.** `subscription.tsx` line ~168 `billing.plan.renegotiate` is STATIC COPY ("contact InnocenZ admin for custom pricing"), not an action. `createAdminRequest` service exists but no UI calls it here. The old memory note calling this "trivial via createAdminRequest" was wrong.
- Still-not-wireable (need backend change): A1 check-in/out (shift-assignment canWrite = admin|agency only — widen to outlet + scope); A2 log-sales (needs per-PR picker UI); release-early/cut-loss (needs new cutlost-request + agency-approval backend); seal shift (needs seal→payroll flow).

Roster Phase-3 (swap / auto-assign / late-flag) — user deferred 2026-07-21, "doing later". Still zero backend.

**When it comes to wiring:** the reverse mapper (draft → `CreateShiftInput`) is the missing half of `backend-shift-map.ts`. Watch the fields the demo composer carries that the `shift` table has NO column for: `payTierRows`, `tierRates`, `eventDrinkMenu`, `dressCode`, `destination`, `preferredStarTiers`, `requestedPrIds`. Decide per field: new column, drop, or keep demo-only. `quantity` / `languages` / `preferredRating` / `payPerHour` / `estimatedCost` map cleanly.

Also: the composer enforces subscription caps (`prPerDayMax`, `prSelectMax`) client-side by counting demo-store shifts. On a backed session those counts must come from the real shift list, or the cap silently checks the wrong data.
