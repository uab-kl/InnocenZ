---
name: outlet-agency-gaps
description: "Portal-specific remaining gaps for Outlet + Agency, repo-verified 2026-07-27 — incl. the dispute 'resolve' button that silently discards a PR's dispute"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6035bb3b-c39d-49a0-92d0-191791dc1ce7
  modified: 2026-07-29T02:07:38.171Z
---

Verified 2026-07-27 (branch SL). Supersedes the "COMPLETE" framing in [[outlet-portal-backend-wiring]] and [[agency-portal-backend-wiring]] — those recorded *wiring* done; this is what is still functionally missing. See also [[backend-gap-audit-verified]] and [[app-foundation-gaps]].

**RE-VERIFIED 2026-07-27 (later pass, HEAD 930ae9d).** Closed since the first pass: outlet swap (2773ec3), agency auto-assign (`use-auto-assign-plan.ts`, b68372f), PR-side dispute raise/withdraw (`POST /payment-voucher/mine/:voucherId/dispute[/withdraw]`).

**AGENCY — worst finding first, and now WORSE.** Migration `0051_pv_dispute_resolution` created `payment_voucher_dispute` (one row per day × component, proof-required CHECK, outcome/resolved_by columns) and commit 6e96a0f added the **model file only** — grep for `paymentVoucherDispute` hits `payment-voucher.model.ts` and nothing else: no repository, no controller, no routes, no frontend. Meanwhile the agency button is unchanged: `resolveDispute: (id) => patch(id, { status: "sent" })` (use-agency-pvs.ts:62, wired at routes/agency/pv.tsx). PRs can now raise real disputes into a table nobody reads, and the agency's only control still discards them. ~~**Top item: dispute repository/controller/routes + agency review queue.**~~ **DONE 2026-07-29** — dispute rewrite (`cad9199`, one row per day × component, proof optional per `0064`) + agency review queue with accept/reject + notify-the-PR (`2982d65`), E2E browser-verified. (Note: journal jumps 0049 → 0051; there is no 0050.)

Other agency gaps: ~~payroll this-week verify surface is hard-blocked on the absent `receipt_no` column~~ **WRONG + NOW DONE — `receipt_no`/`source` exist and always did; surface shipped `0ba5309`, see [[agency-payroll-verify]]**; ~~`DELETE /payment-voucher/:id` is reachable by agency~~ **CLOSED by 2026-07-29 — the route now carries a `canDelete` guard**; no PR self-sign endpoint so the agency sets `prSignedAt` today **(still true 2026-07-29)**; `AgencyGpsPanel` + `GpsRoadMap` exist but read `agency-demo`/`pr-demo`/`lib/gps-locations.ts` and need `GET /shift-assignment/live` ~~which in turn needs check-in to accept coordinates — a 3-layer chain~~ **chain shortened 2026-07-29: check-in NOW accepts `{lat,lng,accuracy}` (validated in `check-in-geofence.ts`), so only the `/live` endpoint + panel wiring remain**; auto-assign scoring absent (`listReplacementCandidates` orders by tier parity only, repository.ts:375, already has 4 web callers to extend); broadcast has no endpoint; collections/reconciliation has no settle/remind/pay/dual-key endpoints; Special Service gated by `agencyCan()` not backend RBAC; shift swaps + PR availability + cut-loss all backend-0 (cut-loss = product call).

**OUTLET — NEW, most severe, found 2026-07-27 (later pass).** The whole `/outlet` router is mounted behind `authenticateJWT` **only** (`router/v1.ts:44`); `outlet.routes.ts` puts `requireAdmin` on just `approve` + `suspend`. `outlet.controller.ts` never reads `req.user` in `create`/`update`/`setGeoFence`/`addMember`/`removeMember`. So ANY authenticated account — including a PR — can edit any outlet, move any venue's geo-fence, add itself as a member of any outlet, and create outlets. `/rating` is the same: `rating.routes.ts` has no role gate and `rating.controller.ts` has zero `req.user` hits, so anyone can post an outlet→PR rating for any pair. This is a bigger hole than the missing sub-role middleware and should be fixed with it.

~~Set-venue-geo-point UI is unbuilt~~ **DONE 2026-07-28 (`574c266`)** — outlet Settings → Attendance card: geocode from the venue's own address or a typed one, pick a candidate (shows coords + Google precision), or enter lat/lng manually; owner-only, matching `outletOwnerOnly`. **Correction to this memory's earlier claim: no pin does NOT block check-in.** `verifyWithinGeoFence` returns `{ok: true, enforced: false}` when `outlet.lat/lng` are null (check-in-geofence.ts:157) — unpinned venues accept every check-in with no location check, which is the permissive failure, not the blocking one. `outlet_penalty_rule` is stored by the outlet-workspace CRUD but never read by any shift/assignment code. Rating window + warn/suspend thresholds have zero enforcement in `features/rating/`. Team invite OTP is fake — `OtpVerifySheet.tsx` + `SecuritySettingsSheets.tsx` import `lib/verify-demo-otp.ts` (same OTP service as PR sign-up: build once, two consumers). Workspace `tip` category is a data change not a schema one — `outlet_drink_menu.category` already exists and `/shift-assignment/mine` already ships the per-outlet menu. Sub-role gating is `outletCan()`/`agencyCan()` frontend-only across 91 usages, but the `sub_role` PG enum columns already exist — only middleware missing.

**Workbook corrections:** `/agency/live` is now just a redirect to `/agency/roster` (routes/agency/live.tsx) — the sheet still describes it as a separate surface. Dead files, safe to delete: `agency-payroll-demo-pvs.ts`, `history-demo-sync.ts` (zero importers). Still live: `demo-seed.ts` is imported by BOTH `routes/agency/route.tsx` and `routes/outlet/route.tsx`. `OutletSalesDashboard` uses `velvet-week-demo` only as an unauthenticated fallback (comment at line 293) — its authenticated path is real.

**Suggested order:** `receipt_no` + `source` migration (unblocks agency payroll verify AND PR OCR simultaneously) → dispute resolve/reject rewrite → outlet geo-point UI.
