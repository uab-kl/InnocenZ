# Copy to android/keystore.properties (gitignored). Never commit the real file or .keystore.
#
# storeFile is relative to android/app/ unless you use an absolute path.
storeFile=innocenz-upload-key.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=innocenz
keyPassword=YOUR_KEY_PASSWORD
