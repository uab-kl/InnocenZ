node-linker=hoisted
# Windows + Expo Metro: hardlinks share inodes with other projects (e.g. OneDrive),
# which can attach reparse tags. Metro then calls readlink() and crashes with EINVAL.
package-import-method=copy
