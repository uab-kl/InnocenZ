DEPLOY_HOST=
DEPLOY_USER=
DEPLOY_DOMAIN=
DEPLOY_SSH_PORT=22
DEPLOY_REMOTE_DIR=~/innocenz-staging
FRONTEND_IMAGE=juneyou/innocenz-frontend:staging
BACKEND_IMAGE=juneyou/innocenz-backend:staging

# Baked into the frontend image at build time (must be STAGING, not production).
# Defaults to https://api.<DEPLOY_DOMAIN>/api if unset.
# VITE_API_URL=https://api.YOUR_STAGING_DOMAIN/api
# VITE_GRAPHQL_ENDPOINT=
# VITE_R2_PUBLIC_URL=https://pub-….r2.dev
# VITE_OUTLET_ROLE_ID=
# VITE_AGENCY_ROLE_ID=
